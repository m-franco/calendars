---
import Home from "../components/pages/Home.astro";
import { alternatesFor, urlFor } from "../i18n/ui";

const alternates = alternatesFor((l) => urlFor(l));
---

<Home lang="es" alternates={alternates} />
